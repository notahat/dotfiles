# Global Copilot Instructions

## Git Branch Naming

- All branch names must start with `notahat/`
- For dependency upgrades: `notahat/upgrade-X-to-Y-in-Z`
  - X = package name, Y = new version, Z = app/directory (e.g. `native`, `api`)
  - Example: `notahat/upgrade-tar-to-7.5.11-in-native`
